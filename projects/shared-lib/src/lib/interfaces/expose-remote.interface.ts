export interface ExposesRemote {
  exposedModule: string; // folder exposed by remote
  path: string; // host path by access remote
  displayName: string; // display link name
  ngModuleName: string; // module exposed by remote
}
